#target illustrator#targetengine maintry{    var doc = app.activeDocument;    var allObj = doc.pageItems;    for(i = 0; i < allObj.length; i++){        if(allObj[i].note == "AkaGroup"){            var orgItem;            var grpItems = allObj[i].pageItems;            for(j = 0; j < grpItems.length; j++){                var obj = grpItems[j];                if(obj.name == "Original"){                    orgItem = obj.pageItems[0];                    break;                }            }            orgItem.move(allObj[i],ElementPlacement.PLACEBEFORE);            grpItems.removeAll();        }    }}catch(e){}