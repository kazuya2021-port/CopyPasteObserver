#target illustrator#targetengine mainfunction setAllObjNote(){    try{        var doc = app.activeDocument;        var allObj = doc.pageItems;        var allObjJson = [];        var objID = 0        for(i = 0; i < allObj.length; i++)        {            try            {                var o = allObj[i];                if(!o.guides)                {                    if(o.note != "AkaGroup")                    {                        allObj[i].note = "scriptSelect" + objID;                        objID++;                    }                }            }            catch(e)            {                $.writeln("error Object:" + objID);            }        }    }    catch(e) {        $.writeln(e);    }}setAllObjNote();